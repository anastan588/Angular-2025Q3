import { FormatvaluesPipe } from './formatvalues-pipe';

describe('FormatvaluesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatvaluesPipe();
    expect(pipe).toBeTruthy();
  });
});
